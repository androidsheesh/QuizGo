[start]
# This boots your 'web' container back up as a web server on port 80
cmd = "node /assets/server.js || php artisan serve --host=0.0.0.0 --port=80"

[processes]
# This defines a separate independent process named 'worker'
worker = "php artisan queue:work --tries=3"
